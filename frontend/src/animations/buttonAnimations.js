const buttonAnimations = {

    pulseButton(buttonId) {

        console.log(
            `Pulse Animation On ${buttonId}`
        );

    },

    loadingButton(buttonId) {

        console.log(
            `Loading Animation On ${buttonId}`
        );

    },

    successAnimation(buttonId) {

        console.log(
            `Success Animation On ${buttonId}`
        );

    }

};

export default buttonAnimations;