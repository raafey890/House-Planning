const hoverEffects = {

    applyCardHover(cardId) {

        console.log(
            `Hover Effect Applied To ${cardId}`
        );

    },

    applyButtonHover(buttonId) {

        console.log(
            `Button Hover Applied To ${buttonId}`
        );

    },

    applyImageHover(imageId) {

        console.log(
            `Image Hover Applied To ${imageId}`
        );

    }

};

export default hoverEffects;