const modalAnimations = {

    openModal(modalId) {

        console.log(
            `Opening Modal ${modalId}`
        );

    },

    closeModal(modalId) {

        console.log(
            `Closing Modal ${modalId}`
        );

    },

    animateBackdrop() {

        console.log("Backdrop Animation Active");

    }

};

export default modalAnimations;